# Remaining TODOs - Orion POS

All Phase 1 to 10 capabilities are fully operational, tested, and build successfully.

## Future Recommendations
- **Real WhatsApp API integration**: Share links are currently generated using `wa.me` links. Future integrations can link a real Meta WhatsApp Business API endpoint.
- **Razorpay production webhook signatures**: Enable verification checks on Razorpay webhook signature headers when moving from sandbox testing to live payment gateways.
- **Super admin access restrictions**: Ensure only the super-user root account can run metrics checks in live staging.
